// Re-use the Open Graph card as the Twitter card image so both social
// platforms get the same preview. Next.js auto-wires <meta name="twitter:image">
// from this file convention.

export { default, alt, size, contentType } from "./opengraph-image";
