// Single source for the brew install command used by the install section,
// the hero CTA, and the final CTA. All Tokenscope data + formatters now
// live in lib/tokenscope-data.ts (snapshot-backed, matches the real app).

export const BREW_CMD = "brew install --cask hdusy/tokenscope/tokenscope";
